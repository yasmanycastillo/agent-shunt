{
  "provider": "gemini",
  "model": "gemini-2.5-flash",
  "base_url": "https://generativelanguage.googleapis.com/v1beta/openai",
  "timeout": 90,
  "max_tokens": 8192,
  "notes": "Provider options: gemini, openai, groq, deepseek, anthropic, openrouter, ollama. You can set model to 'auto' to dynamically pick the best available model for each task. Do NOT put your API key in this file — use environment variables instead (e.g. GEMINI_API_KEY, GROQ_API_KEY, SHUNT_API_KEY). The api_key field exists as a last-resort fallback but is discouraged."
}
